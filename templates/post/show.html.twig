<div class="card">
  <img class="card-img-top" src="{{asset('uploads/'~ post.image)}}" style="height:100px" alt="">
  <div class="card-body">
    <h4 class="card-title">{{post.title}}</h4>
    <p class="card-text">{{post.description}}</p>
  </div>
</div>
